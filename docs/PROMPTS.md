# 메디백 — 자주 쓰는 요청 템플릿

> Claude한테 무언가 시키고 싶을 때 이 파일에서 복사해서 던지세요.
> `{}` 부분만 채워 넣으면 됩니다.

---

## 🛠️ 일상 수정

### 단순 UI 수정
```
{화면 이름}의 {요소}를 {원하는 동작/모양}으로 바꿔주세요.
예: 홈 화면의 검색창 placeholder 색을 더 진하게 해주세요.
```

### 색/간격만 바꾸기
```
{화면}에서 {요소} 색을 theme.js의 {COLORS.xxx}로 바꿔주세요.
```

### 새 필드 추가 (처방전/영양제 등)
```
{도큐먼트, 예: supplements}에 {새 필드명}({타입}) 추가해 주세요.
- 시드 데이터(constants.js)도 같이 추가
- 입력 폼에서 편집 가능하게
- 카드에 노출 {위치}
```

---

## 📱 새 화면/탭 추가

```
{탭 이름} 탭을 하단 네비에 추가하고, 새 화면을 만들어주세요.
- 데이터: Firestore의 `medbag/{key}` 도큐먼트
- 화면 구성: {원하는 레이아웃}
- 시드 데이터: {예시}
- 편집 가능 여부: {예/아니오}
```

---

## 🚀 배포 & 호스팅

### 빌드 + 배포
```
빌드해서 배포해주세요.
```
(자동으로 npm run build && firebase deploy --only hosting 실행)

### Firestore 규칙 변경 + 배포
```
firestore.rules를 {규칙}으로 바꾸고 배포해주세요.
```

### OCR 살리기 (Vercel 경로)
```
OCR을 살리고 싶어요. Vercel에 배포하는 절차 안내해주세요.
(npx vercel login은 제가 직접 합니다.)
```

---

## 🔐 인증/보안 단계 업

```
Firebase Auth(Google 로그인)을 추가해서 본인만 접근 가능하게 만들어 주세요.
- 로그인 안 한 상태면 로그인 화면만 보이게
- Firestore 규칙도 `request.auth.uid == "{내uid}"`로 잠그게
```

---

## 🗃️ 데이터 작업

### Firestore에 있는 내 실데이터 확인
```
지금 Firestore에 저장된 데이터를 보여주세요.
(처방전, 영양제, 아이정보, 메모, 약사노트 전부)
```

### 시드 데이터 비우기
```
constants.js의 시드 데이터(SAMPLE_PRESCRIPTIONS / SAMPLE_SUPPLEMENTS / SAMPLE_PHARMACIST_NOTES)를 빈 배열로 바꿔주세요.
앞으로 처음 들어오는 사용자는 빈 상태로 시작합니다.
```

### 데이터 마이그레이션 / 백업
```
Firestore의 모든 medbag/* 도큐먼트를 JSON으로 백업해서 /tmp나 로컬 파일로 저장해주세요.
```

---

## 🎨 디자인 토큰 수정

```
theme.js의 {COLORS.xxx 또는 GRADIENTS.xxx}를 {새 값}으로 바꿔주세요.
연결된 화면들 자동으로 다 반영됩니다.
```

### 새 토큰 추가
```
theme.js에 {새 토큰 이름}을(를) 추가하고, {적용할 곳}에 일괄 적용해주세요.
```

---

## 🐞 버그 리포트 (캡처와 함께)

```
{화면 이름}에서 {증상}이 발생합니다.
재현: {스텝}
스크린샷 첨부합니다. (또는: 에러 텍스트는 ~~)
원인 찾아서 고쳐주세요.
```

---

## 🔄 동작 변경 (행위 자체)

```
{기능}의 동작을 {원하는 방식}으로 바꿔주세요.
- 변경 전: {현재 동작}
- 변경 후: {원하는 동작}
- 영향 받는 화면: {목록}
```

---

## 📊 새 기능 컨셉 검토 (구현 전 협의)

```
{기능 아이디어}를 추가하고 싶은데, 메디백 구조 안에서 어떻게 넣는 게 가장 자연스러울까요?
- 어디에 넣을지, 데이터 어디에 저장할지, UI 어떻게 보일지 같이 정해주세요.
- 결정되면 구현 들어가요.
```

---

## 🧹 소스 정리

```
{영역}의 코드를 리팩토링해서 정리해주세요. 행위는 그대로 유지.
- 중복 패턴 추출
- 죽은 코드 제거
- theme.js 토큰 적용
- ESLint 통과
```

---

## ✅ 배포 직전 체크리스트 요청

```
배포하기 전에 다음 체크해주세요:
- npm run lint 통과
- npm run build 성공
- 새 기능 동작 확인 (간단히)
체크 후 firebase deploy 진행해주세요.
```

---

## 🤖 자동 업데이트 알림 동작 확인

```
지금 배포한 버전의 version.json과 앱이 인식하는 __BUILD_VERSION__이 일치하는지 확인해주세요.
일치 안 하면 자동 업데이트 배너가 안 뜹니다.
```

---

## 📦 새 라이브러리 추가

```
{라이브러리}를 설치해서 {목적}에 사용하고 싶어요.
번들 사이즈에 큰 영향 있으면 미리 알려주세요.
```

---

## 💡 팁

- **"왜 이게 안 되지?"** 같은 질문은 그냥 자유롭게 던지셔도 됩니다. 위 템플릿은 새 작업 시작할 때만 참고.
- 새 세션 시작 시 [CLAUDE.md](../CLAUDE.md)는 자동 로드됩니다. 그래서 매번 "이 앱은 무슨 앱이고…" 설명할 필요 없음.
- 화면 캡처 첨부는 매우 유효. UI 문제는 글보다 그림이 빠릅니다.
